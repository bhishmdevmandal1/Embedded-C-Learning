
#include <stdio.h>

int main(void)
{
	char ascii1, ascii2, ascii3, ascii4, ascii5, ascii6;

	printf("Enter any 6 character of your choice:");
	fflush(stdout);
	scanf("%c %c %c %c %c %c",&ascii1, &ascii2, &ascii3, &ascii4, &ascii5, &ascii6);

	printf("\nASCII codes : %d,%d,%d,%d,%d,%d",ascii1, ascii2, ascii3, ascii4, ascii5, ascii6);


	printf("\nPress enter key to exit the application");	//For command prompt (.exe file)
	while(getchar() != '\n') 	//To make your application, to wait until the user presses a key from the keyboard. (for hanging the program)
	{
			//just read the input buffer and do nothing
	}
	getchar();

}
